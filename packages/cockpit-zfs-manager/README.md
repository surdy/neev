# cockpit-zfs-manager (DEFERRED)

Not yet referenced by any manifest. Installs a Cockpit ZFS UI from a GitHub
release tarball. Pin and verify `VERSION` against the upstream repo before
adding `cockpit-zfs-manager` to `manifests/standard`.
