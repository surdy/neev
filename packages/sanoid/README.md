# sanoid (DEFERRED)

Not yet referenced by any manifest. The install path relies on the
`ublue-os/staging` COPR which is not guaranteed stable; verify the package
exists and builds before adding `sanoid` to `manifests/standard`.
